import type { GameData } from "../types/game"

const data: GameData = {
    gameWidth: 800,
    gameHeight: 600,
    reelsAmount: 3,
    starterSymbolsData: [0,1,2],
    soundVolume: 100,
    audioManager: null,
    isWebpSupported: false,
}

export default data